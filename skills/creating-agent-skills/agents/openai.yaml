interface:
  display_name: "Creating Agent Skills"
  short_description: "Create, validate, package, and evaluate agent skills."
  default_prompt: "Create or improve an agent skill with eval cases, validation, and package structure."
policy:
  allow_implicit_invocation: true
