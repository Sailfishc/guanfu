{
  "schemaVersion": "2.1.0",
  "app": { "name": "{AppName}", "slug": "{app-slug}" },
  "docRoot": "{doc-root}",
  "targetStack": "{target-stack}",
  "workItems": [
    {
      "id": "tokens",
      "title": "Create design token module",
      "priority": 1,
      "inputs": ["05-design-tokens.json"],
      "outputs": ["src/styles/tokens.css"],
      "acceptance": ["all component styling references token variables"]
    }
  ]
}
